
  # Convert Figma to HTML Tailwind

  This is a code bundle for Convert Figma to HTML Tailwind. The original project is available at https://www.figma.com/design/Zn92dCUEIQptnzykPT2Unu/Convert-Figma-to-HTML-Tailwind.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  